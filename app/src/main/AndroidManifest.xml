<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ottus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ottus">
        <activity android:name=".ActivitysForDetailedFilms.Film4"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ActivitysForDetailedFilms.Film3"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ActivitysForDetailedFilms.Film2"
            android:parentActivityName=".MainActivity"/>

        <activity android:name=".ActivitysForDetailedFilms.Film1"
            android:parentActivityName=".MainActivity"/>


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>